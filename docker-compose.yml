services:
  sql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    platform: linux/amd64
    container_name: sportson-sql
    environment:
      SA_PASSWORD: 'Your_password123!'
      ACCEPT_EULA: 'Y'
    ports:
      - '1433:1433'
